My basic boilerplate project. 

Includes jQuery (1.4.3), jQuery UI (1.8), an XHTML and HTML5 skeleton, and basic css helpers; including a clearfix class (.group) and a standard whitespace reset. 

Protip: Add this base script to your profile.

function starter() {
  git clone git@github.com:ohokay/starter.git $1
}

$ starter <project_name>